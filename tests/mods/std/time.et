#!/usr/bin/env et

import std.time;

start = time.now();

println( 'seconds passed since epoch: ', start );

end = time.now();

println( 'time in microseconds for a println: ', ( end - start ).usecs() );