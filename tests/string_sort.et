str = "string";
len = len( str );

for i = 0; i < len; i += 1 {
	for j = 0; j < len - i - 1; j += 1 {
		if str[ j ] > str[ j + 1 ] {
			tmp = str[ j ];
			str[ j ] = str[ j + 1 ];
			str[ j + 1 ] = tmp;
		}
	}
}

println( str );