#!/.buildfiles/et

import vec;

fn facto( n ) {
	if n < 2 { return 1; }
	return n * facto( n - 1 );
}

if args.len() > 1 {
	fact = facto( 5 );
	assert( fact == 120, 'factorial of 5 should be 120, found: ', fact );
} else {
	println( 'factorial of 5 is: ', facto( 5 ) );
}
