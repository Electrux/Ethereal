#!./buildfiles/et

import vec;
import str;

struct s {
	t = "hi";
	u = 5;
}

a = s{ "hi", 7 };
a.t = "true";
if args.len() < 2 {
	println( a.t );
} else {
	assert( a.t == 'true', 'a.t should have been a string: \'true\'' );
}
