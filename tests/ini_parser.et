#!./buildfiles/et

import ini;

ini = ini_read( 'data/test1_ini_parser.ini' );
assert( ini.blks[ 'owner' ].pairs[ 'name' ] == 'John Doe', 'in tag \'owner\', in pair key \'name\', the value should have been \'John Doe\'' );