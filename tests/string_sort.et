str = "string";
len = len( str );

for i = 0; i < len; ++i {
	for j = 0; j < len - i - 1; ++j {
		if str[ j ] > str[ j + 1 ] {
			tmp = str[ j ];
			str[ j ] = str[ j + 1 ];
			str[ j + 1 ] = tmp;
		}
	}
}

println( str );