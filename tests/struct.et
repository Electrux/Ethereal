#!/usr/bin/env et

import std.vec;
import std.str;

struct s {
	t = 'hi';
	u = 5;
}

a = s{ 'hi', 7 };
b = s{ 'hello', 14 };
b = a;
a.t = 'true';
if args.len() < 2 {
	println( a.t );
} else {
	assert( a.t == 'true', 'a.t should have been a string: \'true\'' );
	assert( b.t == 'hi', 'b.t should have been a string: \'hi\'' );
}
